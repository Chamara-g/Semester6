close all;

figure;
beta = -1.3;
fun_beta(beta);
title('beta = -1.3');

figure;
beta = -0.9;
fun_beta(beta);
title('beta = -0.9');

figure;
beta = 0.8;
fun_beta(beta);
title('beta = 0.8');

figure;
beta = 1.2;
fun_beta(beta);
title('beta = 1.2');